#include <stdio.h>
#include <dlfcn.h>

volatile int x = 500;
char stuff[] = "";

void bigpaddingfunc() {
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
	x += ((x / 20) % 34) ^ 0xffffff;
}

int main(int argc, char** argv) {
	void* handle;
	void* func;
	printf("Super simple program, with symbols\n");
	printf("Loading library\n");
	printf("%d\n", RTLD_NOW);
	handle = dlopen("/home/jordan/darkelf/testing/testso.so", 2);
	if (handle) {
		printf("dlopened\n");
		func = dlsym(handle, "sayit");
	} else {
		printf("%s\n", dlerror());
	}
	printf("Done!\n");
	return 0;
}
